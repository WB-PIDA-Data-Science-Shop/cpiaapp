% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_table.R
\name{create_cpia_table}
\alias{create_cpia_table}
\title{Create CPIA Score Data Table}
\usage{
create_cpia_table(data)
}
\arguments{
\item{data}{Plot data from prepare_plot_data() in long format. Will be automatically
transformed to wide format internally via prepare_table_data().}
}
\value{
A DT datatable htmlwidget object with:
\itemize{
\item Wide format display (years × entities)
\item Export buttons (copy to clipboard, CSV, Excel)
\item Horizontal scrolling for many columns
\item 15 rows per page (paginated)
\item Scores rounded to 1 decimal place
\item No row names displayed
}
}
\description{
Main orchestrator function that creates a formatted interactive data table with
export capabilities. This function handles the full table generation pipeline:
data transformation → DT datatable creation → formatting. The resulting table
allows users to export data to Excel/CSV, sort columns, and paginate through years.
}
\examples{
\dontrun{
# Prepare data with multiple entities
plot_data <- prepare_plot_data(
  data = cpiaetl::standard_cpia,
  group_data = cpiaetl::group_standard_cpia,
  selected_country = "Kenya",
  question = "q12b",
  selected_regions = c("Africa (AFR)")
)

# Create interactive table
table <- create_cpia_table(plot_data)
}

}
\keyword{internal}
