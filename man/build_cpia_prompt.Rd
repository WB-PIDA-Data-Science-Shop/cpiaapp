% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_report.R
\name{build_cpia_prompt}
\alias{build_cpia_prompt}
\title{Build CPIA Report Prompt}
\usage{
build_cpia_prompt(country, question, question_label, plot_data)
}
\arguments{
\item{country}{Character. The selected country name (e.g., \code{"Kenya"}).}

\item{question}{Character. The selected question code (e.g., \code{"q12a"}).}

\item{question_label}{Character. The human-readable question label
(e.g., \code{"Property Rights and Rule-based Governance"}).}

\item{plot_data}{A data frame with columns \code{economy}, \code{year}, \code{score},
\code{display_name}, \code{group_type}, as returned by \code{prepare_plot_data()}.}
}
\value{
A named list with two character elements:
\describe{
\item{\code{system}}{The system prompt defining role, methodology, and style.}
\item{\code{user}}{The user message containing all data for this report.}
}
}
\description{
Constructs a fully populated LLM prompt for generating a CPIA country-level
criterion assessment. Loads the system prompt and style examples from
\verb{inst/prompts/}, then injects the current data context — country, question,
score history, and comparator summary — into the user message.

The function reads directly from the \code{plot_data} tibble already computed by
\code{prepare_plot_data()}, so no additional data wrangling is needed in the
server.
}
\details{
Build a CPIA LLM prompt from reactive plot data
}
\examples{
\dontrun{
mock_data <- tibble::tibble(
  economy      = c("Kenya", "Kenya", "Africa"),
  year         = c(2022L, 2023L, 2023L),
  score        = c(3.0, 3.5, 3.1),
  display_name = c("Kenya", "Kenya", "Africa (AFR)"),
  group_type   = c("Selected Country", "Selected Country", "Comparator")
)
prompt <- build_cpia_prompt("Kenya", "q12a",
                            "Property Rights and Rule-based Governance",
                            mock_data)
names(prompt)  # "system" "user"
}

}
\keyword{internal}
