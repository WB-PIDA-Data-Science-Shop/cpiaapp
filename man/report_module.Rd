% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_module.R
\name{report_module}
\alias{report_module}
\title{Report Module}
\usage{
report_module(id, country, question, question_label, plot_data)
}
\arguments{
\item{id}{Character. The Shiny module namespace ID. Must match the \code{id}
passed to \code{report_ui()}.}

\item{country}{Reactive expression returning the selected country name.}

\item{question}{Reactive expression returning the selected question code.}

\item{question_label}{Reactive expression returning the human-readable
question label.}

\item{plot_data}{Reactive expression returning the prepared plot data frame,
as produced by \code{prepare_plot_data()}.}
}
\value{
Called for side effects only.
}
\description{
Convenience wrapper that pairs \code{report_ui()} and \code{report_server()} into a
single callable function, following the same pattern as \code{viz_module.R}.

Call \code{report_ui(id)} inside the app UI and \code{report_module()} inside the
app server to wire up the full report panel.
}
\details{
AI Report Module
}
\examples{
\dontrun{
# In app server:
report_module(
  id             = "report",
  country        = reactive(input$country),
  question       = reactive(input$question),
  question_label = reactive(question_label()),
  plot_data      = plot_data
)
}

}
\keyword{internal}
