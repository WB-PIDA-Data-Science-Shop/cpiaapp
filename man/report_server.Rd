% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_server.R
\name{report_server}
\alias{report_server}
\title{Report Server}
\usage{
report_server(id, country, question, question_label, plot_data)
}
\arguments{
\item{id}{Character. The Shiny module namespace ID. Must match \code{report_ui()}.}

\item{country}{Reactive expression returning the selected country name
(character scalar, e.g. \code{reactive(input$country)}).}

\item{question}{Reactive expression returning the selected question code
(character scalar, e.g. \code{reactive(input$question)}).}

\item{question_label}{Reactive expression returning the human-readable
question label (character scalar).}

\item{plot_data}{Reactive expression returning the prepared plot data frame,
as produced by \code{prepare_plot_data()}.}
}
\value{
Called for side effects only (standard Shiny module server pattern).
}
\description{
Shiny server function for the AI-generated CPIA assessment module. Handles:
\itemize{
\item Triggering LLM report generation when the "Generate Report" button
is clicked.
\item Streaming the LLM response token-by-token into the UI via
\code{stream_llm_response()}.
\item Managing button state (generating / ready) through reactive flags.
\item Revealing the download button once generation is complete.
\item Producing a Word document for download via \code{format_report_docx()}.
\item Surfacing user-friendly error messages without crashing the session.
}

This module is a \strong{consumer} of the viz module's reactive state. It reads
\code{country}, \code{question}, \code{question_label}, and \code{plot_data} but does not own
or modify any of them.
}
\details{
Report Module Server
}
\keyword{internal}
